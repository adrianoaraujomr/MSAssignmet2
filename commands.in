quit : back out of the program
run  : run the simulation
set  : set a value to a variable $var
	 - list of vars :  nro_eventos - number of events
set_model ["mm1"/"mm2"] : change the queue model
set_dist $var ["det"/"nrm"/"exp"] : set a distribution to a variable $var
	 - det : deterministic
	 - nrm : normal distribution
	 - exp : exponential distribution
	 - list of vars :  tec - event of arrival
			   ts  - event of service
export : generate a txt with data generated by the last simulation
